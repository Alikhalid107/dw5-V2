export const BASE_TYPE1_CONFIG = {
  // ── Garage configuration for base type 1 ─────────────────────────────────
  garage: {
    structureImage: "../public/garage_structure.png",
    doorsImage: "../public/garage_doors.png",
    width: 263,
    height: 181,
    offsetX: -140,        // Horizontal offset from base center
    offsetY: 5,     // Vertical offset from base center
    zIndex: -95,
  },
};
